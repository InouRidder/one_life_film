<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3">
    <%= simple_form_for :password do |f| %>
    <%= f.input :password %>
    <%= f.hidden_field :film_id, value: @film.id %>
    <%= f.submit %>
    <% end %>
    </div>
  </div>
</div>
