<%= f.input :name, :input_html => {class: "form-control", placeholder: "Name" } %>
<% if @booking %>
  <%= f.input :booking_id, input_html: {value: booking.id}, as: :hidden  %>
<% end %>
<%= f.input :video_url, :input_html => {class: "form-control", placeholder: "vimeo url" }, label: "Vimeo link!" %>
<%= f.input :slug, :input_html => {class: "form-control", placeholder: "link" } %>
<%= f.input :password, :input_html => {class: "form-control", placeholder: "password"  } %>
<%= f.input :promo, label: "Vink deze aan als je wil dat de film op de publieke pagina van OneLife Film komt" %>
<%= f.submit class: "btn btn-primary" %>
