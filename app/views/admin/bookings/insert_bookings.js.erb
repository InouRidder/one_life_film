const container = document.getElementById('booking-container');
container.innerHTML = "<%= j render 'bookings', bookings: @bookings %>";
const title = document.getElementById('booking-title');
title.innerHTML = "<%= j @title %>"
