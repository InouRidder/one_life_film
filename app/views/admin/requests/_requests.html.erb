<table class="table table-striped table-hover">
  <thead z>
    <tr>
      <th><h2><%= @title %></h2 id="booking-title"></th>
      </tr>
    </thead>

    <thead class="thead-inverse">
      <tr>
        <th>Naam</th>
        <th>Contact</th>
        <th>Aanvraag ingediend</th>
        <th>Trouw Datum</th>
        <th>Status</th>
        <th>Acties</th>

      </tr>
    </thead>

    <tbody>
      <% requests.each do |request| %>
      <% unless request.cancelled? %>
        <%= render 'show', request: request %>
      <% else %>
        <%= render 'cancelled', request: request %>
      <% end %>
    <% end %>
  </tbody>
</table>
