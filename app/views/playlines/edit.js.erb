var line = document.getElementById('playline-id[<%= @playline.id %>]');
// line.classList.add('pre-animation');
line.innerHTML = "<%= j render 'playlines/form', playbook: @playbook, playline: @playline %>";
// setTimeout(function(){
//     line.classList.remove('pre-animation')
// },4500)
