<li id=song-choice-id[<%= song_choice.id %>]>
  <div class="song-row">
    <%= song_choice.title.upcase %>
    -
    <%= song_choice.artist.upcase %>
    <%= link_to  client_booking_song_choice_path(@booking, song_choice), method: :delete, data: {confirm: "Sure?"}, remote: true do %>
    <button class="select-button">REMOVE</button>
    <% end %>
  </div>
</li>
